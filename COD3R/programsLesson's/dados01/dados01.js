/*
console.log(nome);
console.log(quantidade);
console.log(preco);
console.log(imposto);
console.log(precoFinal);

var nome = "Caneta";
var quantidade = 10;
var preco = 6.4;
var imposto= 1.5;
var precoFinal= preco + imposto;

Deu erro, pois eu devo criar antes de usar.
Mesmo com var, dá undefined.
*/

/*
console.log(nome);
console.log(quantidade);
console.log(preco);
console.log(imposto);
console.log(precoFinal);

let nome = "Caneta";
let quantidade = 10;
let preco = 6.4;
let imposto= 1.5;
let precoFinal= preco + imposto;

Também deu erro, e agora foi erro mesmo
não foi nem undefined.
*/




/*
let nome = "Caneta";
let quantidade = 10;
let preco = 6.4;
let imposto= 1.5;
let precoFinal= preco + imposto;

console.log(nome);
console.log(quantidade);
console.log(preco);
console.log(imposto);
console.log(precoFinal);

Agora deu certo, pois está na ordem correta.
*/

{
  
  var nome = "Caneta";
  var quantidade = 10;
  var preco = 6.4;
  var imposto= 1.5;
  var precoFinal= preco + imposto;
  

}


  {
    
    
    
    console.log(bibi);
    console.log(nome);
    console.log(quantidade);
    console.log(preco);
    console.log(imposto);
    console.log(precoFinal);
    
  }

  
  
  

/**
 * Dentro de blocos apenas o var consegue acessar
 * pois ele é global e local, ou seja, pode criar
 * ele dentro ou fora e chamar dentro ou fora 
 * depois de criado
 * que ele funcionará
 * 
 * Já o let é só local, então, se eu colocasse let
 * como está ai no lugar do var
 * daria erro, pois o let só pode ser excutado
 * dentro do escopo que ele foi criado.
 */

