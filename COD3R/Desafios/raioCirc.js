/**
 * function RaioCirc (raio1) 
   {
      const PI = 3.141592;
      const FORMULA = PI * ( raio1 * raio1);
                
      console.log (FORMULA)

      }

      RaioCirc(20);

      MEU JEITINHO, SEM RETURN

 */

      const PI = 3.141592;
      let raio1= 20;
      const FORMULA = PI * (raio1 * raio1);
      console.log ("O resultado é:" +"" + "" + "" + FORMULA + "" +  "m²");
      let newFormula = Math.PI * (raio1 * raio1);
      console.log ("O resultado é:" +"" + "" + "" + newFormula + "" +  "m²");
// jeitinho do professor!
//obs: Math.PI é o  PI já fixo do JS.
      